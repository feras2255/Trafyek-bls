# دليل الرفع على السيرفر — trafyekbls.com

> اقرأ **§١ الحواجز** قبل أي شيء. رفع الكود قبل تشغيل الهجرات سيكسر نموذج التواصل.

---

## ١. حواجز يجب معالجتها قبل الرفع 🚨

### ١.١ الهجرات لم تُشغَّل بعد — وهذه أخطر نقطة

عشر هجرات في `supabase/migrations/` **لم تُطبَّق على قاعدة البيانات**. التطبيق لا يشغّلها تلقائياً.

**الأثر إن رفعت الكود بدونها:** `contact/actions.js` يكتب هذه الأعمدة:

```
company · service_type · budget_range · status · locale
source_page · utm_source · utm_medium · utm_campaign
```

ولا يوجد أي منها في `contact_messages` قبل الهجرة ٠٠١. النتيجة: **كل رسالة تواصل ترجع خطأ ولا تُحفظ.** أي فقدان كامل للعملاء المحتملين من لحظة الرفع.

### ١.٢ ترتيب التشغيل الإلزامي

شغّلها بالترتيب من Supabase → SQL Editor، **واحدة تلو الأخرى** مع قراءة نتيجة كل واحدة:

| # | الملف | ماذا يفعل | إلزامي قبل الرفع؟ |
|---|---|---|---|
| ٠٠١ | `001_contact_messages_lead_fields.sql` | أعمدة العميل المحتمل | ✅ **نعم — وإلا انكسر النموذج** |
| ٠٠٢ | `002_newsletter_subscribers.sql` | جدول القائمة البريدية | ✅ **نعم — نموذج المدونة يكتب فيه** |
| ٠٠٣ | `003_project_case_study_fields.sql` | حقول دراسة الحالة | لا (اختياري) |
| ٠٠٤ | `004_blog_related_articles.sql` | تصنيف المقالات | لا |
| ٠٠٥ | `005_categories_highlight.sql` | تمييز الخدمة | لا |
| ٠٠٦ | `006_slugs.sql` | أعمدة الروابط الوصفية | لا — **راجع الـ slugs المولّدة قبل الاعتماد عليها** |
| ٠٠٧ | `007_seed_legal_pages.sql` | إنشاء الصفحات القانونية | ✅ **نعم — وإلا روابط الفوتر تعطي 404** |
| ٠٠٨ | `008_city_pages.sql` | جدول صفحات المدن | لا |
| ٠٠٩ | `009_brain_core.sql` | مخزن حقائق المخ التقني | لا |
| ٠١٠ | `010_brain_events.sql` | جدول الأحداث | لا |

**الحد الأدنى للرفع الآمن: ٠٠١ و٠٠٢ و٠٠٧.**

كلها **إضافية فقط** — لا تحذف عموداً ولا تعيد تسمية شيء ولا تلمس بياناتك القائمة. ومع ذلك: **خذ نسخة احتياطية قبل التشغيل** (Supabase → Database → Backups).

### ١.٣ تحقق بعد كل هجرة

```sql
-- بعد ٠٠١
select column_name from information_schema.columns
 where table_name = 'contact_messages' order by column_name;

-- بعد ٠٠٧ — يجب أن تعيد ثلاثة صفوف
select slug from public.pages order by slug;
```

---

## ٢. متغيرات البيئة

في إعدادات المشروع على الاستضافة (Vercel → Settings → Environment Variables):

| المتغير | سري؟ | ملاحظة |
|---|---|---|
| `NEXT_PUBLIC_SUPABASE_URL` | لا | يجب أن يطابق `hostname` في `next.config.mjs → images.remotePatterns` |
| `NEXT_PUBLIC_SUPABASE_ANON_KEY` | لا — يصل المتصفح بالتصميم | كل ما يبلغه محكوم بسياسات RLS |
| `SUPABASE_SERVICE_ROLE_KEY` | ✅ **نعم — يتجاوز RLS بالكامل** | خادمي فقط. لا يُستورد أبداً في ملف فيه `"use client"` |

اضبط الثلاثة على **Production و Preview و Development** معاً، وإلا فشلت معاينات الـ Pull Requests.

⚠️ **مفتاح Google Cloud المكشوف سابقاً لم يُبطَل بعد.** أبطله من Google Cloud Console قبل الرفع.

---

## ٣. النطاق — خطوة ضرورية

الكود يوجّه الآن من النطاق المجرّد إلى `www` (`next.config.mjs → redirects`). لكي يعمل:

1. أضف **كلا** النطاقين في الاستضافة: `trafyekbls.com` و`www.trafyekbls.com`
2. اجعل `www.trafyekbls.com` هو **النطاق الأساسي**
3. تحقق بعد الرفع:

```bash
curl -sI https://trafyekbls.com/ar/services | grep -i "^location"
# المتوقع: location: https://www.trafyekbls.com/ar/services
```

**السبب:** نتائج بحث حقيقية أظهرت صفحات مفهرسة على النطاق المجرّد بينما الموقع يعلن canonical على `www` — أي أن جوجل يرى نطاقين لموقع واحد ويقسم السلطة بينهما.

---

## ٤. الرفع

```bash
npm ci
npm run build      # يجب أن ينتهي بـ ✓ Compiled successfully
```

البناء لا يفشل الآن إذا تعذّر الوصول إلى Supabase — يسجّل الخطأ ويكمل بخريطة موقع أقصر. هذا مقصود: خريطة موقع ناقصة مؤقتاً مشكلة تُصلح نفسها، أما بناء لا يُرفَع فلا.

---

## ٥. التحقق بعد الرفع — نفّذها بالترتيب

```bash
SITE=https://www.trafyekbls.com

# ١) الصفحات الأساسية = 200
for p in /ar /en /ar/about-us /ar/services /ar/blogs /ar/ourwork /ar/contact; do
  echo "$p -> $(curl -s -o /dev/null -w '%{http_code}' $SITE$p)"
done

# ٢) توحيد النطاق
curl -sI https://trafyekbls.com/ar | grep -i "^location"

# ٣) الروابط القديمة
curl -sI $SITE/en/p/lrXGr | grep -i "^location"     # → /en/about-us

# ٤) بطاقة المشاركة: يجب 200 و image/png
curl -s -o /dev/null -w "%{http_code} %{content_type}\n" $SITE/ar/opengraph-image

# ٥) خريطة الموقع تحتوي hreflang
curl -s $SITE/sitemap.xml | grep -c "xhtml:link"    # > 0

# ٦) الصفحات القانونية (بعد الهجرة ٠٠٧)
curl -s -o /dev/null -w "%{http_code}\n" $SITE/ar/pages/terms-conditions
```

**يدوياً:**
- أرسل رسالة من `/ar/contact` وتأكد من ظهورها في `/dashboard/messages`
- شارك رابط الموقع في واتساب وتأكد من ظهور صورة المعاينة
- افتح صفحة خدمة وتأكد من ظهور «أعمالنا في …» إن وُجدت مشاريع مرتبطة

---

## ٦. بعد الرفع مباشرة

| # | الإجراء | لماذا |
|---|---|---|
| ١ | أضف `www.trafyekbls.com` في **Search Console** وارفع `sitemap.xml` | بدونه لا يمكن قياس أي شيء — نصف بنود خطة السيو محجوبة عليه |
| ٢ | من Search Console → Indexing → Pages → **Not found (404)** استخرج قائمة الروابط القديمة | لإكمال خريطة التحويلات في `next.config.mjs` |
| ٣ | أنشئ GA4 واربطها بالحاوية `GTM-K46W3W6N` | الحاوية مركّبة منذ البداية ولا تستقبل حدثاً واحداً |
| ٤ | راقب Console على الصفحة الرئيسية وصفحة التواصل وصفحة خدمة واللوحة | لتفعيل الـ CSP — انظر §٧ |

---

## ٧. تفعيل الـ CSP (بعد أسبوع من الرفع)

سياسة أمان المحتوى تعمل الآن بوضع **Report-Only** — تسجّل المخالفات ولا تمنع شيئاً.

بعد التأكد من خلو الـ Console من مخالفات على: الرئيسية · صفحة خدمة · صفحة التواصل (خريطة جوجل) · لوحة التحكم (رفع الصور) —

غيّر في `next.config.mjs`:

```diff
- { key: "Content-Security-Policy-Report-Only", value: csp },
+ { key: "Content-Security-Policy", value: csp },
```

**لا تفعّلها قبل هذا الفحص.** التفعيل المبكر قد يعطّل خريطة جوجل أو رفع الصور في اللوحة.

---

## ٨. ما لم يُنفَّذ بعد ويحتاج قرارك

| # | البند | لماذا توقف |
|---|---|---|
| ١ | تحويل الروابط الرقمية (`/services/12`) إلى وصفية عربية | تغيير Routing + DB. الهجرة ٠٠٦ جاهزة، وتحتاج مراجعة الـ slugs المولّدة قبل الاعتماد عليها، ثم خريطة 301 كاملة |
| ٢ | صفحة الأسعار / الباقات | قرار تجاري — أوضح فجوة تجارية وجدها التدقيق |
| ٣ | إشعار وصول عميل محتمل (بريد / واتساب) | يحتاج اختيارك للقناة |
| ٤ | حد معدل حقيقي لنموذج التواصل | يحتاج عدّاداً مشتركاً — بنية تحتية لا أختارها نيابةً عنك |
| ٥ | حذف الوحدات الميتة (`ProjectsSlider`, `servicesGrid`, `ProductsCard`) | لا تؤثر على البناء، والحذف يحتاج موافقتك |
| ٦ | فهرس `/cities` وإنهاء يتم صفحات المدن | لا صفحة مدينة منشورة بعد؛ الفهرس الفارغ أسوأ من غيابه |

---

## ٩. حالة التحقق عند كتابة هذا الملف

| الفحص | النتيجة |
|---|---|
| `npx eslint` | ✅ صفر تحذيرات وصفر أخطاء |
| `npm run build` | ✅ `✓ Compiled successfully` |
| المسارات العامة (٩ مسارات) | ✅ كلها 200 |
| الروابط غير الموجودة | ✅ 404 صحيح |
| توحيد النطاق | ✅ 308 بلا حلقة تكرار |
| الروابط القديمة | ✅ تحويل صحيح مع حفظ اللغة |
| `/ar/opengraph-image` | ✅ 200 · `image/png` · `1200×630` |
| `sitemap.xml` | ✅ hreflang لكل رابط |
| البناء مع تعذّر Supabase | ✅ ينجح ويسجّل الأخطاء |

**الأخطاء الوحيدة في سجل الخادم أثناء الفحص كانت حجب الشبكة لـ Supabase في بيئة الاختبار — لا أخطاء برمجية.**
